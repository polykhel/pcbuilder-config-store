server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pcb_auth
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        
logging:
  level:
    org:
      hibernate:
        type: trace